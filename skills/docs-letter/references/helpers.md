# API generate.py - справочник

## Параметры страницы

- Формат: A4
- Поля: верх 2 / лево 3 / право 1.5 / низ 2 см
- Шрифт: Times New Roman 14pt (тело), 11pt (реквизиты в шапке), 8pt (футер)
- Межстрочный интервал: одинарный (1.0)
- Красная строка: 1.25 см
- Выравнивание: по ширине (тело), по центру (обращение, реквизиты)

## Импорт

```python
import sys
sys.path.insert(0, "skills/docs-letter")
from generate import create_letter
```

## Параметры create_letter()

| Параметр | Тип | Описание |
|----------|-----|----------|
| `addressee_lines` | `list[str]` | Строки адресата. Пустая строка = визуальный разрыв |
| `greeting` | `str` | Обращение. По умолчанию «Уважаемые коллеги!» |
| `body_paragraphs` | `list[str \| dict]` | Абзацы тела (подробнее ниже) |
| `appendix_text` | `str` | Текст приложения (без слова «Приложение:»). Пусто = не добавлять |
| `signer_title` | `str` | Должность подписанта |
| `signer_name` | `str` | И.О. Фамилия подписанта |
| `doc_date` | `str` | Дата «ДД.ММ.ГГГГ». Пусто = подчеркивание |
| `doc_number` | `str` | Номер документа. Пусто = подчеркивание |
| `on_number` | `str` | «На №» для ответных писем. Пусто = строка «на №» не выводится (инициативное письмо) |
| `executor_name` | `str` | ФИО исполнителя (Фамилия Имя Отчество) |
| `executor_title` | `str` | Должность исполнителя (опционально). Если указана, выводится отдельной строкой между ФИО и телефоном |
| `executor_phone` | `str` | Телефон исполнителя |
| `output_path` | `str` | Путь сохранения |

## Формат body_paragraphs

Каждый элемент - один абзац. Два формата:

**Строка** - обычный абзац с красной строкой:
```python
body_paragraphs=[
    "Первый абзац текста.",
    "Второй абзац текста.",
]
```

**dict** - абзац с дополнительными опциями:
```python
body_paragraphs=[
    "Обычный абзац.",
    {"text": "Заголовок раздела", "bold": True, "center": True, "indent": False},
    {"text": "Нумерованный пункт", "numbered": True},
    {"text": "Второй нумерованный", "numbered": True},
    {"text": "Буллет с тире", "dashed": True},
    {"text": "Абзац без красной строки", "indent": False},
]
```

Ключи dict:
- `text` (str) - текст абзаца
- `indent` (bool, default True) - красная строка 1.25 см
- `numbered` (bool) - нумерованный список Word (1., 2., 3., ...). Каждая группа подряд идущих `numbered` элементов стартует с 1.
- `dashed` (bool) - маркированный список Word с «–» (en-dash). Все группы используют один list-стиль; автопунктуация (`;` после каждого, `.` после последнего в группе)
- `bold` (bool) - жирный
- `center` (bool) - по центру

Списки реализованы через стандартный механизм Word (`numbering.xml`, `<w:numPr>`), а не как обычный текст с префиксом. Отступ маркера и текста: hanging 0.5 см (709 dxa).

## Формат адресата

Каждая строка - отдельный элемент списка `addressee_lines`:
```python
addressee_lines=[
    "Генеральному директору",
    "ФГБУ «Научный центр»",
    "",                        # пустая строка = визуальный разрыв
    "[Фамилия И.О.]",          # инициалы ПОСЛЕ фамилии (ГОСТ Р 7.0.97-2016)
]
```

ФИО адресата — в дательном падеже, инициалы после фамилии («Фамилии И.О.»).
Это отличается от блока подписи, где формат «И.О. Фамилия» (инициалы перед фамилией).

## Шапка-бланк (левая колонка)

Строится автоматически из `~/.docs-plugin/org_details.md`. Поведение:

- **Вышестоящий орган** (`parent_org` / `parent_org_short`) — выводится ПРОПИСНЫМИ буквами (ГОСТ Р 7.0.97-2016), 11pt bold, по центру.
- **Реквизиты** (`address`, `phone_fax`, `email`, `okpo`/`ogrn`, `inn_kpp`) — как заданы в конфиге, без переформатирования. Адрес и телефон правятся ТОЛЬКО в `org_details.md`.
- **Построчная вёрстка бланка** (опционально): ключи `header_name_lines` и `header_address_lines` в `org_details.md` задают разбиение наименования и адреса на отдельные центрированные строки (разделитель — «|»), как на официальном бланке. Префикс «адрес:» добавляется к первой строке адреса. Если ключи не заданы — наименование и адрес выводятся одной строкой с авто-переносом. Канонические `full_name`/`address` при этом не меняются (их используют docs-ord и docs-protocol).
- **Референс**: при пустых дате и номере — `от ____________№ ________`.
- **«на № … от …»** — выводится ТОЛЬКО для ответных писем (когда задан `on_number`). Инициативные письма эту строку не содержат.

## Исполнитель

Всегда размещается в теле документа после блока подписи, отделён 5 пустыми строками.

- Серый текст (#808080, 8pt), Times New Roman
- Без `executor_title`: строки «ФИО», «телефон»
- С `executor_title`: строки «ФИО,», «должность,», «телефон»

В колонтитул письма исполнитель не выносится.
